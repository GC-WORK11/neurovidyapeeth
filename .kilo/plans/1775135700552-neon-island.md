# NVP Landing Page Frontend Updates

## Overview
Update the NeuroVidyaPeeth landing page with a new circular logo, remove promotional images, refine the hero section, and improve button aesthetics to match a premium education startup feel. Light theme throughout — no cyberpunk, neon, retro, or cheap styling.

## Changes

### 1. Logo Replacement (Nav + Footer)
- **File**: `src/pages/LandingPage.tsx`
- Copy `Extract_the_circular_logo_without_altering_it_no_i_delpmaspu-modified (1).png` to `src/assets/nvp-logo-circular.png`
- Replace the import `nvpLogo` to point to the new circular logo file
- Nav bar (line 268): Update `<img>` to use the circular logo with appropriate sizing (`h-12 w-12 rounded-full object-cover` to emphasize the circular shape aesthetically)
- Footer (lines 571-575): Replace the `<BookOpen>` icon + text block with the same circular logo image, styled consistently with the nav

### 2. Remove Hero Section Images
- **File**: `src/pages/LandingPage.tsx`
- Remove the two image cards (lines 348-361) that display `aimlImg` and `promptImg`
- Remove the imports for `aimlImg` and `promptImg` (lines 30-31) since they'll be unused

### 3. Expand & Refine Hero Section
- **File**: `src/pages/LandingPage.tsx` (lines 312-369)
- With images removed, expand the hero content to fill the space naturally:
  - Keep the badge, heading, and description
  - Add a clean key-features row below the description (using existing data — no dummy text): pull from the `stats` array or `highlights` to show 3-4 quick facts inline as small pill-style items
  - Keep the light theme: `bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-indigo-50/50 via-white to-white` is fine
  - Remove the decorative blob elements (lines 366-368) — they contribute to the "AI slop" aesthetic the owner criticized

### 4. Button Redesign
- **File**: `src/pages/LandingPage.tsx`
- **"Take Test — ₹99" button** (hero + nav):
  - Rename to **"Eligibility Test — ₹99"** (professional, clear, education-startup appropriate)
  - Style: Clean solid button with subtle shadow, remove excessive ring/gradient effects. Use `bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg shadow-md hover:shadow-lg transition-all`
  - Update in both hero section and nav (desktop + mobile)
- **"Inquire Now" button** (hero):
  - Rename to **"Request Details"** (more professional than "Inquire Now")
  - Style: Outlined/ghost style to differentiate from primary CTA. Use `border border-slate-300 text-slate-700 hover:bg-slate-50 rounded-lg` — clean, not flashy
  - Remove `shadow-xl` and scale effects that feel cheap
- **"Apply Now" button** (nav):
  - Rename to **"Apply Now"** → keep as is or change to **"Start Application"** for consistency
  - Style: Keep as the secondary nav CTA but tone down the shadow

### 5. Highlights Section Theme Fix
- **File**: `src/pages/LandingPage.tsx` (lines 423-472)
- The "Why Choose This Program?" section currently uses a dark `bg-[#1a1a1a]` theme which clashes with the light theme request
- Convert to light theme: `bg-white` or `bg-slate-50` with appropriate text color changes
- Keep the layout and content, just swap colors to match the overall light aesthetic

### 6. Cleanup Unused Imports
- **File**: `src/pages/LandingPage.tsx`
- Remove unused imports: `aimlImg`, `promptImg`, `BookOpen` (replaced by logo image in footer)

## Files to Modify
1. `src/pages/LandingPage.tsx` — all changes
2. `src/assets/nvp-logo-circular.png` — new file (copy of the provided PNG)

## No Changes To
- Curriculum data, highlights data, terms, inquiry form logic, stats, footer links, contact info, terms & conditions
- No dummy text or false information added
- No new sections added beyond reorganizing existing content
